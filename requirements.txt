# Core — used by both the CLI and the desktop app
google-api-python-client>=2.120.0
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
Jinja2>=3.1.3
PyYAML>=6.0.1
rich>=13.7.0
dnspython>=2.6.0          # MX lookups: catches addresses that would bounce
ruamel.yaml>=0.18         # comment-preserving YAML writes from the settings UI

# Desktop app only (Windows). The CLI and the test suite run without it.
pywebview>=5.0

# Tests
pytest>=8.0
